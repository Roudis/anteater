# Exercise 1 — Terraform

Deploys `acmehealth-raw-claims`, deliberately public. See
`../../../book/src/03_exercise_one.md` for what to build and why — don't
fix the flaw here until the book tells you to.

```bash
terraform init
terraform apply
```
